---
    title: Introduction to MDX
    summary: MDX is a powerful tool that allows you to write JSX in your Markdown.
    image: /images/posts/posty1.jpg
    author: 'Brandon Funk'
    publishedAt: '2024-08-08'
---

Mdx is a powerful tool that allows you to write JSX in your markdown files. 

## What is MDX

MDX is a file format that allows you to write JSX in your markdown files. This maens you can use react components, import other components, and even write inline JSX in your markdown files.

<br/>
```
export async function getSource() {
    const response = await fetch('https://api.example.com/data');
    const data = await response.json();
    return data;
}
```
<br/>

## Why use MDX

MDX is a greate way to write interactive documentation, blog posts, and other stuff and things. Do things and other stuff.
<br/>
<Counter />